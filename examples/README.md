# examples

these examples are here for two reasons:

1. show what a good filled-out spec looks like
2. show when a spec is still too weak and the AI should ask questions

## files
- `habit-tracker-spec.md` — small single-user app example
- `bug-board-spec.md` — small internal tool example
- `incomplete-spec-example.md` — intentionally weak example that should trigger follow-up questions

steal the structure.
not the exact app idea.
